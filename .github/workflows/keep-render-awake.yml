# .github/workflows/keep-render-awake.yml
name: Keep Render Awake
on:
  schedule:
    - cron: "*/10 * * * *"   # every 14 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://infero-finance.onrender.com/health || true
